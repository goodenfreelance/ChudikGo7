# Подробное руководство по физике и биомеханике "Чудиков"

Данный документ содержит полное техническое и математическое описание физического движка, используемого для симуляции биомеханики, движения и взаимодействия чудиков на клеточном поле.

---

## 1. Геометрия и Анатомия Чудика

Чудик представляет собой сочлененную биомеханическую структуру, состоящую из следующих базовых элементов:

1. **Ребра (Каркас / Плечи):**
   - Отрезки длины $1$ клетка (прямые) или $\sqrt{2}$ клетки (диагональные).
   - Каждое ребро обладает **массой $m_i = 1.0$**.
2. **Шарниры (Сочленения / Узлы):**
   - Точки соединения ребер на сетке.
   - Имеют **массу $m_{joint} = 0.0$**.
3. **Мышцы (Обычные и Случайные 🎲):**
   - Элементы, создающие активный сгибающий/распрямляющий момент вокруг шарнира.
   - **Обычные мышцы** срабатывают каждый симуляционный шаг.
   - **Случайные мышцы** срабатывают вероностно с заданным шансом $P \in [4\%, 20\%]$.

---

## 2. Расчет Массы, Центра Масс и Момента Инерции

### 2.1 Суммарная Масса ($M$)
Общая масса чудика определяется как сумма масс всех его составных ребер:
$$M = \sum_{i=1}^{N} m_i = N \cdot 1.0$$
где $N$ — количество ребер в теле чудика.

### 2.2 Момент Инерции ($I$)
Момент инерции чудика относительно его геометрического центра рассчитывается как:
$$I = \sum_{i=1}^{N} m_i \cdot r_i^2$$
где $r_i$ — расстояние от центра масс чудика до $i$-го элемента. Если момент инерции не задан явно, он аппроксимируется формулой:
$$I \approx M \cdot R_{bounds}^2 \cdot 0.5$$

---

## 3. Кинематика и Биомеханика Движения

### 3.1 Расчет Силы и Направления Вращения (Правило Легкого Плеча)
Каждая активная мышца создает усилие относительно соответствующего шарнира:
1. Чудик делится шарниром на **Левое плечо** (масса $m_{left}$) и **Правое плечо** (масса $m_{right}$).
2. **Правило легкого плеча:** Вращается то плечо, масса которого меньше ($m_{light} < m_{heavy}$). При равенстве масс ($m_{left} = m_{right}$) выбор вращающегося плеча осуществляется с равной вероятностью $50/50$.

### 3.2 Оценка Силы Вращения и Баланс
- Каждое ребро, расположенное после шарнира, дает **$1$ балл вращательного момента**:
  - $1$ ребро $\rightarrow$ угол поворота $45^\circ$;
  - $2$ ребра $\rightarrow$ угол поворота $90^\circ$.
- **Разность моментов и поступательное движение:**
  - Если на разных шарнирах действуют противоположные моменты (например, $3$ ребра с одной стороны и $2$ с другой):
    - Сбалансированная часть ($\min(3, 2) = 2$) превращается в **поступательный импульс вперед** (+2 шага/прироста скорости);
    - Разность моментов ($3 - 2 = 1$) дает **поворот ориентации тела на $45^\circ$**.

### 3.3 Скорость Движения ($v_{forward}$)
Движение чудика происходит **только при наличии действующих мышц**. Без активных мышц скорость чудика равна нулю.
- Базовый физический шаг вперед равен $v_{forward} = 0.25$ клетки за фазу сокращения/распрямления.
- Направление движения задается углом ориентации головы чудика $\theta$.
- Вектор скорости:
  $$v_x = v_{forward} \cdot \cos(\theta)$$
  $$v_y = v_{forward} \cdot \sin(\theta)$$

---

## 4. Физический Движок Столкновений (Collision Engine)

### 4.1 Радиусы Касания
- **Радиус отдельной детали:** $r_{element} = 0.5$ клетки (полклетки).
- **Радиус касания детали и еды:** $r_{food\_contact} = 0.7$ клетки ($0.5$ деталь + $0.2$ еда).
- **Дистанция соприкосновения двух деталей:** $D_{touch} = r_{element} + r_{element} = 1.0$ клетка.

### 4.2 Двухэтапная Детекция Столкновения
1. **Broad-phase (Грубая проверка):** Проверяется расстояние между центрами чудиков $A$ и $B$:
   $$\sqrt{(X_B - X_A)^2 + (Y_B - Y_A)^2} < R_A + R_B$$
2. **Narrow-phase (Точная проверка деталей):** Вычисляются мировые координаты всех деталей $p_A \in A$ и $p_B \in B$. Находится пара ближайших деталей с минимальным расстоянием:
   $$d_{min} = \min_{p_A, p_B} \| p_B - p_A \|$$
   Если $d_{min} < D_{touch}$ ($1.0$ клетка), фиксируется факт соприкосновения.

---

## 5. Законы Сохранения Импульса и Моментов при Ударе

### 5.1 Нормаль Касания и Относительная Скорость
Вектор нормали удара $\vec{n}$ направлен от точки контакта детали $A$ ($p_A$) к точке контакта детали $B$ ($p_B$):
$$\vec{n} = \frac{p_B - p_A}{\|p_B - p_A\|} = (n_x, n_y)$$

Проекции скоростей чудиков на нормаль:
$$v_{An} = \vec{v}_A \cdot \vec{n}, \quad v_{Bn} = \vec{v}_B \cdot \vec{n}$$
Относительная скорость сближения:
$$v_{rel} = v_{An} - v_{Bn}$$

### 5.2 Ударный Импульс ($J$)
При $v_{rel} > 0$ (тела сближаются) рассчитывается ударный импульс по закону сохранения импульса ($p = m \cdot v$) с учетом коэффициента упругости $e = 0.5$:
$$J = \frac{(1 + e) \cdot v_{rel}}{\frac{1}{m_A} + \frac{1}{m_B}}$$

### 5.3 Позиционное Разделение (Anti-Overlap)
При пересечении тел на величину $\delta = D_{touch} - d_{min} > 0$ производится позиционный сдвиг чудиков без искусственного асимметричного закручивания, пропорциональный их массам:
$$\Delta \vec{x}_A = -\vec{n} \cdot \delta \cdot \frac{m_B}{m_A + m_B}$$
$$\Delta \vec{x}_B = +\vec{n} \cdot \delta \cdot \frac{m_A}{m_A + m_B}$$

### 5.4 Крутящий Момент и Изменение Угла Ориентации ($\tau$)
При нецентральном ударе возникает вращательный момент вокруг центров масс чудиков:
- Плечо удара для $A$: $\vec{r}_A = p_A - C_A = (r_{Ax}, r_{Ay})$
- Плечо удара для $B$: $\vec{r}_B = p_B - C_B = (r_{Bx}, r_{By})$

Сила импульса на $A$: $\vec{J}_A = -J \cdot \vec{n}$; на $B$: $\vec{J}_B = +J \cdot \vec{n}$.
Крутящий момент:
$$\tau_A = r_{Ax} \cdot J_{Ay} - r_{Ay} \cdot J_{Ax}$$
$$\tau_B = r_{Bx} \cdot J_{By} - r_{By} \cdot J_{Bx}$$

Поворот угла ориентации тела:
$$\Delta \theta_A = \text{Clamp}\left( \frac{\tau_A}{I_A} \cdot k, -25^\circ, +25^\circ \right)$$
$$\Delta \theta_B = \text{Clamp}\left( \frac{\tau_B}{I_B} \cdot k, -25^\circ, +25^\circ \right)$$

---

## 6. Управление Панелями и Обзор Поля

Для максимального удобства наблюдения за симуляцией все панели на поле оборудованы кнопками скрытия:
- **Панель управления (сверху по центру):** Кнопка $\uparrow$ сворачивает панель в компактную плавающую кнопку.
- **Панель статистики (справа):** Кнопка $\rightarrow$ сворачивает правый блок в боковую вкладку.
- **Интерфейс игрока / Ручное управление (слева вверху):** Кнопка $\uparrow$ сворачивает джойстик чудика.
- **Шапка приложения:** Кнопка $\uparrow$ полностью убирает верхний баннер.
